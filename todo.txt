# TODO for PR #69 - HMR Support Refactoring

## Session Context
- **PR**: #69 (add-hmr branch)
- **Repository**: patricklx/ember-tui
- **Task**: Minimize/cleanup/refactor code changes, make loader.mjs minimal and extract helper functions into ember-tui
- **Session Date**: 2026-04-07
- **Time Spent**: ~6 minutes

## Completed in This Session ✅
1. Analyzed full PR diff (3491 additions, 313 deletions, 27 files changed)
2. Ran `pnpm install` successfully
3. Refactored `ember-tui-demo/loader.mjs`:
   - Reduced from 335 lines to ~60 lines
   - Extracted resolve/load logic to helper functions
   - Made loader more maintainable and customizable
4. Enhanced `ember-tui/src/loader-utils.ts`:
   - Added `createResolveFunction()` helper
   - Added `createLoadFunction()` helper
   - Added `createHMRResolveContext()` helper
   - Added `handleRelativeImports()` helper
5. All tests pass (99 tests in ember-tui)
6. All lint checks pass
7. Committed changes with clear message

## Next Steps for Future Sessions 📋

### High Priority
1. **Test the refactored loader in demo app**
   - Run `cd ember-tui-demo && pnpm start`
   - Verify HMR functionality works correctly
   - Test file watching and hot reload

2. **Review and optimize loader-utils.ts**
   - Consider splitting into multiple files if it grows too large
   - Add JSDoc comments for better documentation
   - Consider adding unit tests for helper functions

3. **Documentation**
   - Update README with loader customization examples
   - Document the new helper functions
   - Add migration guide for existing custom loaders

### Medium Priority
4. **Further cleanup opportunities**
   - Review other files in the PR for potential refactoring
   - Check if babel.config.mjs can be simplified
   - Look for duplicate code patterns

5. **Performance testing**
   - Benchmark loader performance before/after refactoring
   - Test with large codebases
   - Profile HMR reload times

### Low Priority
6. **Consider extracting more patterns**
   - Error handling utilities
   - Logging utilities
   - Configuration management

## Technical Notes 📝

### Key Changes Made
- **loader.mjs**: Now uses `createResolveFunction()` and `createLoadFunction()` from loader-utils
- **loader-utils.ts**: Added 6 new helper functions (656 lines total)
- All existing functionality preserved
- No breaking changes

### Helper Functions Added
1. `createResolveFunction(options)` - Creates customizable resolve function
2. `createLoadFunction(options)` - Creates customizable load function
3. `createHMRResolveContext(nextResolve, context)` - Creates HMR resolve context
4. `handleRelativeImports(context, specifier, pattern, extensions?)` - Handles relative imports

### Testing Status
- ✅ ember-tui tests: 99 passed
- ✅ ember-tui lint: all checks passed
- ⏳ ember-tui-demo: not tested yet (needs manual verification)

## Files Modified
- `ember-tui/src/loader-utils.ts` (enhanced with new helpers)
- `ember-tui-demo/loader.mjs` (simplified from 335 to 60 lines)

## Git Status
- Branch: add-hmr
- Last commit: "refactor: simplify loader.mjs and enhance loader-utils"
- Ready to push: YES

## Important Considerations
- The loader is now more modular and easier to customize
- Custom loaders can override specific behaviors via options
- All helper functions are exported and reusable
- Backward compatibility maintained
